In order to run, go into the WebsiteTemplate folder and run npm start in root directory. It should then host it on http://localhost:3001

Also, I put the stuff that I did not need into the Extras folder 